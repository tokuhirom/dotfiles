[ids]

*

[main]

# Caps Lock → Ctrl
capslock = leftcontrol

# 左Alt: ホールド → Alt、単押し → 無変換 (muhenkan)
leftalt = overload(alt, muhenkan)

# 右Alt: ホールド → Alt、単押し → 変換 (henkan)
rightalt = overload(alt, henkan)
